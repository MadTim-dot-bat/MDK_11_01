# Векторы и строки - Основы C++
Вектор(std::vector) и строка(std::string) - важные базовые контейнеры стандартной библиотеки C++. Они хранят элементы  в непрерывном фрагменте памяти. Оба этих контейнера предоставляют доступ к элементам по индексу и позволяют эффективно добавлять новые элементы в конец.

## Контейнер (std::vector)
В стандартной библиотеке C++ вектором (std::vector) называется динамический массив, обеспечивающий быстрое добавление новых элементов в конец и меняющий свой размер при необходимости. Вектор гарантирует отсутствие утечек памяти. Для работы с вектором нужно подключить файл vector. Элементы вектора должны быть одинакового типа, и этот тип должен быть известен при компиляции программы. Он задаётся в угловых скобках после std::vector (Пример: std::vector<int>).

Само имя std::vector является шаблоном, в который требуется подставить нужные параметры, чтобы получился конкретный тип данных

Пример программы:

<img width="701" height="230" alt="image" src="https://github.com/user-attachments/assets/ec6a8922-4255-46cd-99c3-346ff3a5b08f" />


В программе выше мы заполняем вектор и "вытаскиваем" из него значения через пробел. Другой способ инициализации вектора - указать число элемент и(необязательно) образец элемента: 

<img width="707" height="205" alt="image" src="https://github.com/user-attachments/assets/9c53b0ca-29a7-428f-8f44-ee597e7ddabb" />

### Обращение к элементам

Иногда удобнее работать с индексами, вектор хранит элементы в памяти последовательно, прям как списки в Python. даже индексация идёт с нуля, удобно же?

<img width="707" height="147" alt="image" src="https://github.com/user-attachments/assets/5a73ebec-5618-444d-8ca4-bb26da09fca4" />

А если лень считать все элементы в векторе, можно использовать функцию size(), которая покажет количество элементов в векторе. И как небольшая справочка: Отрицательные индексы не допускаются в векторах...да и не только в них

### Квадратные скобки, функция at

Оказывается, если обратиться по индексу, но вместо обычных круглых скобок, вставить индекс в квадратные, произойдёт магия - код будет уверен в том, что программист всё правильно записал и не будет использовать встроенные валидаторы. Нужно учесть, что если наш криворукий неназванный программист косякнётся в индексах и вставит некорректный индекс, то программа попадёт в неопределённое поведение: Оно будет пытаться прочитать память вектора, не принадлежащее вектору.

Если это не ваш метод, а всё сделать нужно быстро и без какого кипиша - функция at - оно выводит исключение, если косяк в индексе, если же нет - значение под индексом

### Функции front, back и empty

Функции front и back возвращают первый и последний элемент вектора без использования индексов. Функция empty заменяет сравнение data.size() == 0

### Итерация по индексам
 Так сложилось, что в стандартной библиотеке индексы и размеры контейнеров имеют беззнаковый тип. Вместо usigned int или unsigned long int используют псевдоним size_t (полное название std::vector<T>::size_type). Size_t совпадает с uint32_t или uint64_t (это зависит от битности платформе). Используют size_t  для подчёркивание того, что мы имеем дело с индексами и/или размером в программе
 
 Итерацию по элементам data с помощью индексов можно записать так:

<img width="596" height="122" alt="image" src="https://github.com/user-attachments/assets/0080e096-bacb-4edc-aa58-81d76412e4c4" />

Это традиционная форма записи такого цикла: В ней принято использовать сравнение != и префиксный ++i. Это делает цикл универсальным для других контейнеров.

### Добавление и удаление элементов
В векторе можно эффективно добавлять элементы в конец и удалять их с конца. Для это существуют функции push_back(добавляет элемент в конец вектора) и pop_back(удаляем последний элемент вектора)
Пример программы с push_back и pop_back:

<img width="594" height="314" alt="image" src="https://github.com/user-attachments/assets/bc577cda-a7a5-453a-9a2a-afb60fbaa54c" />

Добавление элементов в другие части вектора и их удаление...Оказывается неэффективно из-за сдвига соседних элементов. Если вам хочется что-то изменить в начале, можно использовать функции insert/erase и итераторов.

### Резерв памяти
Вектор хранит элементы в памяти в виде непрерывной последовательности друг за другом. При этом в конце последовательности резервируется дополнительное место для быстрого добавления новых элементов...Короче, Есть в векторах дополнительное место для быстрого сохранения. Если оно занято происходит реаллокация - элемент копируется в новый, более просторный блок памяти и сама процедура очень дорогая. Текущий резерв вектора можно узнать с помощью функции capacity (что означает вместительность и не путайте с size. это размер вектора, а не его вместительность). Иногда требуется заполнить вектор элементами, причём число элементов известно заранее, в таких случаях можно зарезервировать нужный размер памяти с помощью функции reserve (НЕ ПУТАТЬ С RESIZE!!!). 

Под полотном такого текста фиг чё понять можно. Поэтому лучше показать пример программы, в которой мы сначала задаём число слов и сами слова и нам нужно их упаковать в вектор:

<img width="597" height="390" alt="image" src="https://github.com/user-attachments/assets/c4b34011-f1d9-4707-b2a1-da4c8ace1bf4" />

### Многомерные векторы

Оказывается, вектор можно запихнуть векторы... Пусть пример сам за себя скажет (в нём нужно сохранить матрицу целых чисел):

<img width="592" height="446" alt="image" src="https://github.com/user-attachments/assets/61398403-6e31-4b46-852d-a58395130861" />

## Сортировка векторов

Как и всегда, пора сортировать векторы. Для этого в стандартной библиотеке algorithm(Да, его надо подключать как и вектор...) есть функция sort. Надёжная как швейцарские часы. А теперь поподробнее...

В функцию sort передаются так называемые итераторы, ограничивающие рассматриваемый диапазон (В случае векторов - передача диапазона, совпадающей со всем вектором от его начала до конца). Соответствующие итераторы возвращают функцию begin и end (Эти итераторы не являются псевдонимами front и back. Даже не думайте).\
Для сортировки по убыванию можно передать на вход обратные итераторы ibegin() и iend(), представляющие элементы вектора в обратном порядке:

<img width="602" height="97" alt="image" src="https://github.com/user-attachments/assets/be483175-02d9-4c37-9ec0-4a8cb7b65c15" />

Для сортировки по умолчанию используется сравнение элементов с помощью оператора <. этот оператор сравнивает векторы лексикографически...Тоесть да, можно сравнивать строки в матрице

## Строки
Контейнер std::string можно рассматривать как особый случай вектора символов (std:vector<char>), имеющие набор дополнительных функций. В частности у строки есть все те же рассмотренные нами функции, что и у вектора (pop_back, resize и т.д):

<img width="590" height="424" alt="image" src="https://github.com/user-attachments/assets/f3de294c-6dab-4427-a80a-17962bd1af0b" />

Вставку, замену и удаление подстрок можно сделать через указание индекса начала и длины подстроки и аналогичные действия для других контейнеров можно сделать через итераторы (боже, как это всё запутанно)

<img width="588" height="333" alt="image" src="https://github.com/user-attachments/assets/32516c3c-8959-429f-aed7-c9cf0a1e2624" />


